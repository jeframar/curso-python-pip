# MÓDULO 07 — Retos
# Escribe tu solución debajo de cada enunciado.

# ── Reto 1 ──────────────────────────────────────────────────
# Crea una función llamada `es_par(n)` que devuelva True si n es par.
# Pruébala con varios números.
# Tu código aquí:


# ── Reto 2 ──────────────────────────────────────────────────
# Crea una función `factorial(n)` que calcule n! (factorial).
# n! = 1 * 2 * 3 * ... * n
# Ejemplo: factorial(5) → 120
# Tu código aquí:


# ── Reto 3 ──────────────────────────────────────────────────
# Crea una función `contar_vocales(texto)` que devuelva
# cuántas vocales (a, e, i, o, u) tiene el texto.
# Debe ignorar mayúsculas/minúsculas.
# Tu código aquí:


# ── Reto 4 ──────────────────────────────────────────────────
# Crea una función `es_palindromo(texto)` que devuelva True
# si la palabra se lee igual al derecho y al revés.
# Ejemplo: "radar" → True, "Python" → False
# Tu código aquí:


# ── Reto 5 ──────────────────────────────────────────────────
# Crea una función `calcular_imc(peso_kg, altura_m)` que devuelva el IMC.
# Fórmula: imc = peso / altura**2
# Y otra función `clasificar_imc(imc)` que retorne:
#   < 18.5 → "Bajo peso"
#   18.5-24.9 → "Normal"
#   25-29.9 → "Sobrepeso"
#   >= 30 → "Obesidad"
# Prueba con peso=70, altura=1.75
# Tu código aquí:


# ── Reto 6 (desafío) ────────────────────────────────────────
# Crea la función `es_primo(n)` que determine si un número es primo.
# Un número es primo si solo es divisible entre 1 y él mismo.
# Luego imprime todos los primos del 1 al 50.
# Tu código aquí:
